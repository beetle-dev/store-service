spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc :postgresql://localhost:5432/cafe
    username: admin
    password: password

  data:
    redis:
      host: localhost
      port: 6379
      password: password

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false